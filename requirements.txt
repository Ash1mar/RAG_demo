fastapi>=0.110
uvicorn[standard]>=0.30
pydantic>=2.7

# Embedding & vector search (CPU)
sentence-transformers>=2.2.2
faiss-cpu
requests>=2.31

# Milvus client (skip on Windows to avoid build issues)
pymilvus==2.4.3 ; platform_system != "Windows"

# For request bodies / uploads (optional)
python-multipart>=0.0.9

# Keyword/BM25
rank-bm25>=0.2.2

# Test dependencies (NL→JSON→SQL tests, FastAPI TestClient)
pytest>=7.0
httpx>=0.27

# Local tasks store uses Python stdlib `sqlite3`; no extra package required.
# SQL Server tasks store (requires ODBC driver installed on host)
pyodbc>=5.1
# SQL AST parsing / transpiling
sqlglot>=28.0.0
